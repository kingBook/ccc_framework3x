import { _decorator } from 'cc';
import { StateLevelEnd } from './StateLevelEnd';
const { ccclass, property } = _decorator;

@ccclass('StateLevelFailure')
export class StateLevelFailure extends StateLevelEnd {
    
}